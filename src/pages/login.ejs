<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/signup.css">
    <title>Login Page</title>
    <script>
        console.log(numTriesExceeded);
    </script>
</head>
<body>
    <div class="register-wrapper">
        <center>
        <div class="register-block">
            <h3 class="register-title">Welcome</h4>
            <p class = "sm">Login and book on the go!</p>
        
            <form action="/custregister_submit/custlogin_verify" method="POST">
            <input type="email" name="email" placeholder="Email address" required  autocomplete="off">
            <input type="password" name="password" placeholder="Password" required autocomplete="off">
            <div class="btn_container">
                <%if (numTriesExceeded) { %>
                    <div class = "msg_box">Number of tries exceeded. Return to homepage</div>
                    <% } %>
                <% if (!numTriesExceeded) {%>
                <input type="submit" value="Login"/>
                <%} %>
                <%if (!loginCorrect) { %> 
                    <div class="msg_box">Login Incorrect</div>
                <%}%>
            </div>
            </form>
        <button class = "button" onclick="location.href='/'"><span>Back to homepage</span></button>
        </div>
        </center>
    </div>
</body>
</html>